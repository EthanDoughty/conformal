# Generated by conformal-migrate
# Source: multi_assign_indexed.m
import numpy as np

A[i - 1], _, B[j - 1, k - 1] = f(x)
a, b = x.shape
c[k - 1], d.f = g(x)
q, A[-1], s.f = h(x)
srt = np.sort(v)
idx = np.argsort(v) + 1
B[i - 1] = np.sort(v)
idx2 = np.argsort(v) + 1
_mret = g2(x)
setattr(s, fld, _mret[0])
a2 = _mret[1]
# MATLAB: [varargout{1:nargout}] = wrapped(x) (slice target: no tuple-assignment equivalent)
