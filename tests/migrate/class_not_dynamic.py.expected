# Generated by conformal-migrate 3.4.0
# Source: class_not_dynamic.m
import numpy as np

def check(x):
    t = type(x).__name__
    if type(x).__name__ == 'double':
        y = np.logical_not(x)
    else:
        y = np.logical_not(x > 0)
    s.lambda_ = 1
    s.import_ = 2
    z = getattr(x, '<dynamic>')
    w = obj.method()
    return y
