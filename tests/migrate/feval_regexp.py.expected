# Generated by conformal-migrate 3.4.0
# Source: feval_regexp.m
import numpy as np
import re

def feval_regexp(x, pat, rep):
    y = sin(x)
    z = cos(x, 2)
    m = re.search('\\w+', 'hello world')
    tokens = re.findall('\\d+', 'abc123')
    parts = re.split(',', 'a,b,c')
    cleaned = re.sub('\\s+', ' ', 'foo  bar')
    result = y
    return result
